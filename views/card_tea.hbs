<script defer src="/js/delete_entry.js"></script>
<script defer src="/js/add_comment.js"></script>
<hr>
<div><a id="breadCrumbs" href="/">Вернуться на главную</a></div>
<div><h2>{{entry.title}} 🌱</h2></div>
<div><h3>{{entry.description}}</h3></div>
<img id="entryImg" src="{{entry.file}}">
<div><h3>Место культивации: {{entry.location}}</h3></div>
{{#if admin}}<div id="uploadFile">
  <form ref='uploadForm' 
      id='uploadForm' 
      action='/entry/photo/{{entry._id}}' 
      method='POST' 
      encType="multipart/form-data">
        <input type="file" name="sampleFile"/>
        <div><input type='submit' class="registerbtn" value='Загрузить изображение' /></div>
    </form>
    </div>{{/if}}
<div id="addNewComments">
  {{#if comments}}
  <ul>
    {{#each comments}}
    <li><b>{{this.author}}</b>
      <p>{{this.body}}</p>
      <p>{{this.createdAt}}</p>
    </li>
    {{/each}}
  </ul>
  {{/if}}
  <div>
    <button id="addComment">Добавить комментарий</button>
  </div>
      <div>
      <form id="addForm" style="display: none;">
        <input name="comment" id="comment" type="text">
        <input style="display: none" name="entryId" value={{entry._id}}></input>

        <button class="registerbtn" type="submit" id="comment_button">Оставить комментарий</button>

      </form>
    </div>
  {{#if admin}}<form id="deleteEntry">
      <input style="display: none" name="entryId" value={{entry._id}}></input>
      <button id="deleteButton" type="submit">Удалить запись</button>
    </form>{{/if}}
</div>

